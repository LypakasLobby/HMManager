package com.lypaka.hmmanager.HMs.Fly;

import com.lypaka.hmmanager.HMs.HMSettings;

import java.util.List;

public class FlySettings extends HMSettings {

    public FlySettings (List<String> files, String noPermissionMessage, String useMessage, String universalPermission, boolean requireMove) {

        super(files, noPermissionMessage, useMessage, universalPermission, requireMove);

    }

}
